import React from 'react';
import {ImageSourcePropType, View} from 'react-native';
import {styles} from './styles/styles';
// import ConfirmImage from '../../organisms/camera/ConfirmPhoto';

export default function ConfirmImageScreen(): JSX.Element {
  // const [imagePath] = React.useState<ImageSourcePropType>('');
  return (
    <View style={styles.root1}>
      {/* <ConfirmImage imagePath={imagePath} /> */}
    </View>
  );
}
